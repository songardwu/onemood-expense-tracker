# HANDOFF — 2026-04-15

## 專案概述

出帳管理系統（Onemood ERP）— 室內設計公司 Onemood 的內部請款提報 + Excel 匯出工具。

- **線上網址：** https://onemood-expense-tracker.vercel.app
- **GitHub：** https://github.com/songardwu/onemood-expense-tracker
- **技術棧：** Flask + Jinja2 + Neon Postgres + Vercel Serverless
- **管理員登入：** onemood / admin123（上線後須更換）

---

## 目前進度

### V1（已完成 ✅）
- 提報 CRUD、Excel 匯出（明細 + 總覽）、RWD 手機/桌面雙版面
- 匯款日期 inline 編輯
- 部署到 Vercel

### V2（已完成 ✅，驗收未完整跑通 ⚠️）
- 使用者認證（登入/登出、Flask session 7天過期）
- 角色權限（admin / designer）
- 資料隔離（設計師只看自己、管理員看全部）
- 帳號管理（新增/停用/啟用/重設密碼）
- Excel 管理員版含「提報人」欄位 + 按提報人彙總
- Navbar 顯示使用者名稱 + 管理員專屬連結

### 尚未完成
- PRD V2 驗收標準第 7、8 步（跨使用者操作應回 403，目前靜默失敗）
- 完整 13 步驗收流程未手動跑通
- Onemood 管理員密碼未更換
- SECRET_KEY 未設定 Preview / Development 環境

---

## 異動檔案清單

| 檔案 | 狀態 | 說明 |
|------|------|------|
| `app.py` | V2 改寫 | 全部路由加認證、資料隔離、帳號管理、Excel 升級（504 行） |
| `migrate_v2.py` | V2 新增 | DB 遷移腳本（已執行完畢，僅供參考） |
| `templates/login.html` | V2 新增 | 登入頁（獨立頁面，不繼承 base.html） |
| `templates/users.html` | V2 新增 | 帳號管理頁（admin only） |
| `templates/base.html` | V2 修改 | 新增 navbar（使用者名稱、帳號管理連結、登出） |
| `templates/list.html` | V2 修改 | admin 條件顯示「提報人」欄位 |
| `templates/new.html` | 未改 | 接收 user 變數供 navbar 使用 |
| `static/style.css` | V2 修改 | 新增 navbar、login、users 頁面樣式（+208 行） |
| `.env.local` | V2 修改 | 新增 SECRET_KEY |
| `prdv2.md` | V2 新增 | V2 產品需求文件 |
| `sddv2.md` | V2 新增 | V2 系統設計文件 |
| `taskv2.md` | V2 新增 | V2 任務拆解清單 |
| `vercel.json` | 未改 | |
| `requirements.txt` | 未改 | flask, psycopg2-binary, python-dotenv, pandas, openpyxl |
| `api/index.py` | 未改 | Vercel serverless 入口 |

---

## 已確認事項

| 項目 | 驗證方式 | 結果 |
|------|---------|------|
| 登入頁渲染 | curl /login | ✅ 200 |
| 未登入跳轉 | curl / | ✅ 302 → /login |
| 正確帳密登入 | onemood/admin123 | ✅ 進入清單頁 |
| 錯誤帳密提示 | 錯誤密碼 / 不存在帳號 | ✅ 顯示「帳號或密碼錯誤」 |
| Navbar 使用者名稱 | admin 登入 | ✅ 顯示 Onemood |
| Navbar 管理連結 | admin vs designer | ✅ admin 有、designer 無 |
| 設計師存取 /users | designer_a 登入 | ✅ 導回首頁 |
| 資料隔離（設計師） | designer_a 看不到 admin 資料 | ✅ |
| 資料隔離（管理員） | admin 看到所有人資料 | ✅ |
| 提報人欄位（清單） | admin 清單頁 | ✅ 有「提報人」欄位 |
| 新增帳號 | 建立 designer_a | ✅ |
| 停用帳號 | 停用 designer_a → 登入被擋 | ✅ 顯示「此帳號已停用」 |
| 啟用帳號 | 重新啟用 designer_a | ✅ |
| 重設密碼 | admin 重設 designer_a 密碼 | ✅ 新密碼可登入 |
| Excel 匯出 | admin 下載 | ✅ 有明細+總覽、提報人欄位 |
| 登出 | /logout | ✅ 跳回 /login |
| Vercel 部署 | production | ✅ 線上可登入 |
| **跨使用者刪除回 403** | PRD 第 8 步 | ❌ 靜默失敗，未回 403 |
| **跨使用者存取回 403** | PRD 第 7 步 | ❌ 無單筆查看 API，間接通過 |

---

## 資料庫現況

- **users 表：** 2 筆（Onemood admin + 1 位設計師）
- **reports 表：** 9 筆（皆為正式資料）
- **測試資料：** 已清除

---

## 接手指南

### 先看什麼
1. `prdv2.md` — V2 完整需求，特別注意第 9 節驗收標準（13 步）
2. `app.py` — 所有商業邏輯集中在此，約 504 行
3. `TODO20260415.md` — 待辦優先順序與阻塞點

### 本地開發
```bash
# 安裝依賴
pip install flask psycopg2-binary python-dotenv pandas openpyxl

# 需要 .env.local（含 POSTGRES_URL + SECRET_KEY）
# 可從 Vercel 拉取：npx vercel env pull .env.local

# 啟動
python app.py
# → http://127.0.0.1:5000
```

### 部署
```bash
npx vercel --prod
```

### Git 分支
- `master` — 目前線上版本（V2 已合併）
- `v2-dev` — V2 開發分支（已合併至 master，可刪除）

---

## 架構筆記

- **無 ORM** — 全部用 psycopg2 raw SQL，SQL 集中在 app.py 各路由內
- **Session 機制** — Flask signed cookie，不依賴 server-side store，適合 serverless
- **密碼加密** — werkzeug.security（PBKDF2），非明碼
- **資料隔離** — SQL WHERE 層級，非中間件過濾
- **Excel** — pandas + openpyxl，BytesIO 串流輸出，不落地檔案
- **RWD** — CSS media query 769px 分界，手機卡片 / 桌面表格雙版面
