# Dawn 案場損益管理系統 — System Design Document
> Version: 2026-05-03 | Status: Production (Vercel) | 主要更新：invoices 模組 + UI 8 項改善

---

## 1. 系統概述

Dawn 是為室內設計公司打造的案場損益管理、費用報帳與客戶請款系統。核心目標：

- **報帳管理**：設計師線上提報費用，管理員審核、匯出匯款單
- **案場損益**：追蹤每案場合約收入、成本支出、分潤結算
- **客戶請款**（新）：管理對客戶的請款單，多案場 1:N，自動建議金額
- **廠商管理**：維護匯款帳戶資料，自動帶入報帳表單
- **使用者管理**：角色權限（admin / designer）、登入追蹤

---

## 2. 技術架構

| 層級 | 技術 |
|------|------|
| 後端 | Python 3.x + Flask (Blueprint) |
| 模板 | Jinja2 SSR（無 SPA / 無前端框架） |
| 資料庫 | PostgreSQL (Neon hosted + PgBouncer pooler) |
| 部署 | Vercel Serverless (`api/index.py` 入口) |
| 安全 | flask-wtf (CSRF) + flask-limiter (Rate Limit) + werkzeug (password hash) |
| 匯出 | pandas + openpyxl |
| 前端互動 | Vanilla JS（無框架，僅在 invoices form / 廠商相似偵測使用） |

### 2.1 部署架構

```
Browser → Vercel Edge → Serverless Function (api/index.py)
                              ↓
                        Flask App (app.py)
                              ↓
                    Neon PostgreSQL (PgBouncer pooler)
```

### 2.2 專案結構

```
├── api/index.py              # Vercel serverless 入口
├── app.py                    # Flask 主程式（設定、中介層、Blueprint 註冊）
├── routes/
│   ├── __init__.py
│   ├── auth.py               # 登入/登出/改密碼/登入紀錄
│   ├── reports.py            # 報帳 CRUD / 搜尋篩選 / 匯出 / 加總
│   ├── vendors.py            # 廠商 CRUD / 匯入匯出 / 軟刪除
│   ├── users.py              # 使用者 CRUD
│   ├── projects.py           # 案場損益（收入/押金/成本/分潤）
│   └── invoices.py           # 客戶請款單（新增模組）
├── services/
│   ├── __init__.py
│   └── utils.py              # DB 連線 / 認證裝飾器 / 分頁 / 樂觀鎖 / 審計 / 假日
├── templates/                # 17 個 Jinja2 模板
│   ├── base.html             # 共用佈局（branded navbar / Dark Mode / fade-in）
│   ├── login.html            # 登入頁（漸層背景 + tagline）
│   ├── list.html             # 報帳列表
│   ├── new.html              # 新增報帳
│   ├── vendors.html          # 廠商管理
│   ├── users.html            # 使用者管理
│   ├── projects.html         # 案場列表
│   ├── project_detail.html   # 案場明細（含損益情感色 + 收款進度條）
│   ├── project_form.html     # 新增/編輯案場
│   ├── project_logs.html     # 案場修改紀錄
│   ├── cost_categories.html  # 成本科目管理
│   ├── change_password.html
│   ├── login_logs.html
│   ├── pagination.html       # 分頁元件（include）
│   ├── invoices.html         # 請款單列表（新）
│   ├── invoice_form.html     # 請款單新增/編輯（新）
│   └── invoice_detail.html   # 請款單檢視（新）
├── static/
│   ├── style.css             # 暖大地色系 + Bento Grid + Dark Mode + 動畫
│   └── app.js                # 廠商相似偵測（vendor_form 用）
├── migrations/
│   ├── 001_create_projects.sql
│   └── 002_create_invoices.sql  # 新
├── migrate_v2.py             # users 表 + reports.user_id
├── migrate_v3.py             # reports 鎖定欄位 + vendor_keywords
├── migrate_v4.py             # vendors 表 + reports.payment_method
└── migrate_v5.py             # invoices + invoice_lines + project_payments.invoice_id（新）
```

---

## 3. 資料庫 Schema（13 張表）

### 3.1 reports（報帳紀錄）

| 欄位 | 型別 | 說明 |
|------|------|------|
| id | SERIAL PK | |
| vendor | TEXT NOT NULL | 廠商名稱 |
| vendor_type | TEXT NOT NULL | 廠商類型 |
| amount | NUMERIC NOT NULL | 金額 |
| category | TEXT NOT NULL | 案場成本 / 管銷 / 獎金 |
| invoice_no | TEXT | 發票號碼（可空） |
| invoice_date | DATE NOT NULL | |
| remit_date | DATE | 預設匯款日 |
| project_no | TEXT NOT NULL | 案件編號 |
| stage | TEXT | 階段 |
| user_id | INTEGER NOT NULL FK→users | 提報人 |
| payment_method | VARCHAR(20) | 付款方式 |
| is_locked | BOOLEAN DEFAULT FALSE | 鎖定狀態 |
| updated_by | INTEGER FK→users | 修改人 |
| updated_at | TIMESTAMP | |
| created_at | TIMESTAMP DEFAULT NOW() | |

### 3.2 users（使用者）

| 欄位 | 型別 | 說明 |
|------|------|------|
| id | SERIAL PK | |
| username | VARCHAR(50) UNIQUE NOT NULL | |
| display_name | VARCHAR(100) NOT NULL | |
| password_hash | VARCHAR(255) NOT NULL | bcrypt |
| role | VARCHAR(20) NOT NULL | 'designer' / 'admin' |
| is_active | BOOLEAN DEFAULT TRUE | |
| created_at | TIMESTAMP DEFAULT NOW() | |

### 3.3 vendors（廠商）

| 欄位 | 型別 | 說明 |
|------|------|------|
| id | SERIAL PK | |
| name | VARCHAR(200) UNIQUE NOT NULL | |
| bank_name | VARCHAR(200) NOT NULL | |
| bank_code | VARCHAR(50) NOT NULL | |
| account_no | VARCHAR(50) NOT NULL | |
| account_name | VARCHAR(200) NOT NULL | |
| is_active | BOOLEAN DEFAULT TRUE | 軟刪除 |
| created_by | INTEGER FK→users | |
| created_at | TIMESTAMP DEFAULT NOW() | |
| updated_by | INTEGER FK→users | |
| updated_at | TIMESTAMP | |

### 3.4 vendor_keywords

`id`, `keyword VARCHAR(50) UNIQUE NOT NULL` — 用於去除廠商通用字後比對核心名稱（公司、工作室、設計、工程…）。

### 3.5 projects（案場主檔）

主要欄位：`id`, `case_id` (CASE-YYYYMMDD-NNN), `case_name`, `owner_*`, `contract_date`, `construction_*`, `status`, `designer_id`, 收入欄位（`system_furniture_amount` / `non_system_furniture_amount` / `tax_amount`）、押金（`deposit_amount` / `deposit_refund` / `deposit_status`）、分潤（`profit_share_pct` / `bonus_checked` / `bonus_disbursed` / `bonus_report_id`）、`created_at` / `updated_at`。

### 3.6 project_adjustments / project_discounts / project_payments / cost_categories / project_costs

詳見 `migrations/001_create_projects.sql`。

**重要更新**：`project_payments` 加上 `invoice_id INTEGER FK→invoices ON DELETE SET NULL`（向下相容）。

### 3.7 audit_logs

`id`, `table_name`, `record_id`, `field_name`, `old_value`, `new_value`, `changed_by FK→users`, `changed_at`, `reason TEXT`。

### 3.8 login_logs

`id`, `user_id`, `username`, `success`, `ip_address`, `created_at`。

### 3.9 ⭐ invoices（客戶請款單主檔，新）

| 欄位 | 型別 | 說明 |
|------|------|------|
| id | SERIAL PK | |
| invoice_no | VARCHAR(20) UNIQUE NOT NULL | INV-YYYYMM-NNN |
| client_name | VARCHAR(200) NOT NULL | 客戶名稱 |
| client_address | VARCHAR(300) | 寄送地址 |
| period_year | INTEGER NOT NULL | 請款年 |
| period_month | INTEGER NOT NULL | 請款月 |
| total_amount | NUMERIC(12,2) DEFAULT 0 | 從 lines 加總（後端計算後寫回） |
| status | VARCHAR(20) DEFAULT 'draft' | draft / issued / paid / void |
| issued_by | INTEGER FK→users | |
| issued_at | TIMESTAMP | |
| notes | TEXT | |
| created_at | TIMESTAMP DEFAULT NOW() | |
| updated_at | TIMESTAMP DEFAULT NOW() | |

### 3.10 ⭐ invoice_lines（請款單明細，新）

| 欄位 | 型別 | 說明 |
|------|------|------|
| id | SERIAL PK | |
| invoice_id | INTEGER NOT NULL FK→invoices ON DELETE CASCADE | |
| project_id | INTEGER FK→projects ON DELETE SET NULL | 對應案場（可空，允許自訂行） |
| description | VARCHAR(300) NOT NULL | |
| suggested_amount | NUMERIC(12,2) | 系統建議金額（留檔對照） |
| amount | NUMERIC(12,2) NOT NULL | 實際開立金額 |
| sort_order | INTEGER DEFAULT 0 | |
| created_at | TIMESTAMP DEFAULT NOW() | |

### 3.11 索引（v5 新增）

```sql
idx_invoices_period           ON invoices(period_year, period_month)
idx_invoices_status           ON invoices(status)
idx_invoice_lines_invoice     ON invoice_lines(invoice_id)
idx_invoice_lines_project     ON invoice_lines(project_id)
idx_project_payments_invoice  ON project_payments(invoice_id)
```

---

## 4. 安全機制

| 機制 | 實作 |
|------|------|
| CSRF | flask-wtf CSRFProtect，所有 POST 表單含 `csrf_token()` |
| Rate Limit | flask-limiter，登入 5/min，全域 120/min |
| Session | HttpOnly + SameSite=Lax + Secure(Vercel) |
| 密碼 | werkzeug bcrypt hash，最低 6 字元 |
| 權限 | `@login_required` / `@admin_required` 裝飾器 |
| Session Fixation | 登入前 `session.clear()` |
| HTTP Headers | X-Content-Type-Options: nosniff, X-Frame-Options: DENY, CSP |
| 廠商匯入 | 檔案 ≤ 2MB、≤ 500 筆、格式 .xlsx/.csv |
| 樂觀鎖 | `expected_updated_at` 比對防併發覆寫（案場編輯） |
| 軟刪除 | `vendors.is_active` 旗標取代硬刪除 |
| **invoices 權限** | designer 只能看含自己案場的請款單（EXISTS 子查詢過濾） |

---

## 5. 核心功能模組

### 5.1 報帳模組 (`routes/reports.py`)
- 新增 / 編輯 / 鎖定報帳紀錄
- 搜尋篩選（日期區間 / 廠商 / 案場 / 類別）
- SQL 分頁（LIMIT/OFFSET + COUNT），50 筆/頁
- 廠商加總（GROUP BY vendor + payment_method）
- Excel 匯出（含篩選條件）
- 台灣假日工作日計算（次月 5 日匯款，遇假日順延）

### 5.2 案場損益模組 (`routes/projects.py`)
- 案場 CRUD（自動流水號 CASE-YYYYMMDD-NNN）
- 合約收入：系統/非系統 + 5% 營業稅
- 追加減 / 折讓 / 收款明細
- 裝修押金管理（pending / partial / refunded）
- 成本管理（15 個預設科目）
- 損益計算（毛利 / 利潤率 / 設計師獎金，全 Decimal 精度）
- 獎金核對 → 出帳（建立報帳紀錄）
- 樂觀鎖 + 審計日誌

### 5.3 ⭐ 客戶請款模組 (`routes/invoices.py`)（新）
- 編號自動生成 `INV-YYYYMM-NNN`
- 9 個路由：list / new / create / detail / edit / update / status / delete + suggest API
- 一張請款單列多案場（invoice_lines 1:N）
- 半自動金額：API 算 `settlement_price - total_received`，可手動覆蓋
- 行明細支援自訂行（project_id 可空）
- 狀態機：`draft → issued → paid → void`（草稿可編刪、開立後僅能流轉）
- 每次狀態變更寫 `audit_logs`
- 開立 / 標收款 / 作廢均有 JS confirm 二次確認
- 對應收款顯示（透過 `project_payments.invoice_id` join）
- 列表支援年/月/狀態三條件篩選 + 分頁
- 權限：admin 全 CRUD、designer 唯讀自己案場相關

### 5.4 廠商模組 (`routes/vendors.py`)
- 新增 / 行內編輯 / 軟刪除（啟用/停用 toggle）
- Excel/CSV 批次匯入（Upsert）
- 範本下載 + API `/api/vendor-bank?name=` 自動帶入匯款資訊
- 廠商相似偵測（去除關鍵字後核心字串比對 + 最低 2 字元門檻）
- Flash message 顯示匯入結果

### 5.5 使用者模組 (`routes/users.py`)
- 新增使用者（admin only）
- 停用/啟用帳號 toggle
- 修改密碼 / 重設密碼

### 5.6 認證模組 (`routes/auth.py`)
- 登入 / 登出
- 登入紀錄 + IP 追蹤
- 修改密碼

---

## 6. UI/UX 設計（2026-05-03 改造後）

| 項目 | 規格 |
|------|------|
| 設計系統 | 暖大地色系 Design Token |
| 排版 | Bento Grid 全頁面 |
| 字型 | Noto Serif TC（標題）+ Noto Sans TC（內文） |
| Dark Mode | 暖棕色版，prefers-color-scheme 自動切換 |
| 響應式 | Desktop table ↔ Mobile cards 切換 |
| 導航 | 桌面 navbar **森林綠品牌化** + active 高亮 / 手機 hamburger nav |
| 無障礙 | Skip link / semantic / aria / scope col / WCAG AA |
| 動態 | **fadeInUp 進場 + details 三角箭頭 + 卡片懸停**，prefers-reduced-motion guard |
| 列印 | Print stylesheet（hide nav/buttons） |
| iOS | input font-size ≥ 16px 防縮放 + safe-area |
| 空狀態 | **5 個 inline SVG 插圖**（出帳/案場/廠商/紀錄等） |
| 互動回饋 | 按鈕 hover shadow lift / 表格 zebra stripe / Bento cell 浮起 |
| 數據視覺 | 損益 dashboard **正負情感色** + 收款 **gradient 進度條** |

---

## 7. 環境變數

| 變數 | 用途 | 狀態 |
|------|------|------|
| `SECRET_KEY` | Flask session 簽名 | Production / Development [OK]，Preview 阻塞 |
| `POSTGRES_URL` / `DATABASE_URL` | Neon 連線字串 | [OK] |
| `VERCEL` | 偵測 Vercel 環境（啟用 Secure cookie） | 自動 |
| `FLASK_DEBUG` | 開發模式 debug=True | 預設關閉 |

---

## 8. 已知限制 / 技術債

- DB 連線使用 Neon PgBouncer pooler，不支援 LISTEN/NOTIFY
- Vercel Serverless 無持久記憶體，flask-limiter 使用 `memory://`（跨 instance 不共享）
- 廠商相似偵測為伺服器端比對，非即時前端提示
- `reports.vendor` 為純文字，未與 `vendors` 表 FK 關聯
- 台灣假日資料僅到 2027 年底，2027 Q4 前需更新
- 測試腳本因 CSRF 無法直接以 HTTP client 執行（需 Selenium / playwright）
- invoices 模組無樂觀鎖（draft 編輯多人衝突機率低）
- API `/api/invoice-suggest/<pid>` 為單筆查詢，未做 batch（多選會多次請求）

---

## 9. Migration 歷史

| 版本 | 內容 | 已執行 |
|------|------|--------|
| v2 | users 表 + reports.user_id | [OK] |
| v3 | reports 鎖定欄位 + vendor_keywords | [OK] |
| v4 | vendors 表 + reports.payment_method | [OK] |
| **v5** | **invoices + invoice_lines + project_payments.invoice_id** | **[OK] 2026-05-03** |
| 001 | projects 主檔 + 5 個衍生表 + 9 個科目預設 + audit_logs | [OK]（手動執行 SQL） |
| 002 | invoices schema（與 v5 對應，文件版） | [OK] |

---

## 10. 統計（2026-05-03）

| 指標 | 數量 |
|------|------|
| Blueprint 數 | 6 |
| 路由總數 | ~45 |
| 模板數 | 17 |
| DB 表數 | 13 |
| Migration 腳本 | 5（v2-v5 + 001/002 SQL） |
| 程式碼行數 | ~3,500（routes + services + app） |
| CSS 行數 | ~1,850 |
| 累計 commits | 35（master 分支） |
