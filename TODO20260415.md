# TODO — 2026-04-15

## 下一步行動（依優先順序）

### P0 — 安全性修補（阻塞驗收）

1. **DELETE / UPDATE 路由未回傳 403**
   - 目前設計師對別人的提報執行刪除或修改匯款日期時，SQL `WHERE user_id = %s` 不匹配只是靜默失敗（0 rows affected），不會回傳 403。
   - PRD V2 驗收標準第 7、8 步明確要求回 403。
   - **修法：** 在 `delete()` 和 `update_remit_date()` 中，先 SELECT 檢查 report 歸屬，不匹配時 `abort(403)`。
   - **檔案：** `app.py` 第 233–273 行

### P1 — 驗收流程未完整跑通

2. **跑 PRD V2 第 9 節 13 步完整驗收**
   - 其中第 7、8 步依賴 P0 修完才能通過。
   - 建議用兩個瀏覽器（或無痕模式）手動操作一遍。
   - 需建立 designer_a / designer_b 測試帳號。

### P2 — 管理員密碼安全

3. **Dawn 管理員密碼仍為 `admin123`**
   - 上線後應立即更換。可透過帳號管理頁面自行重設，或在 DB 直接更新 password_hash。

### P3 — 部署環境補強

4. **SECRET_KEY 只設了 Production 環境**
   - Preview / Development 環境尚未設定 SECRET_KEY，Vercel Preview Deploy 會 fallback 到 `dev-secret-change-me`。
   - **修法：** `vercel env add SECRET_KEY preview` + `vercel env add SECRET_KEY development`

### P4 — V3 待辦（不急）

5. 設計師自行修改密碼頁面
6. Audit Log（誰改了什麼）
7. 可編輯已提報項目
8. 登入紀錄 / 登入 IP 追蹤

---

## 阻塞點

| 阻塞項 | 影響範圍 | 解法 |
|--------|---------|------|
| 刪除/修改路由無 403 | 驗收第 7、8 步無法通過 | 加 SELECT 前查 + abort(403) |
| Dawn 預設密碼未更換 | 線上帳號安全 | 手動到帳號管理頁重設 |
| SECRET_KEY 只有 Production | Preview deploy session 不安全 | vercel env add 補齊 |
