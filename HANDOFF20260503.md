# HANDOFF — 2026-05-03

> 客戶請款單模組（invoices）動工日：schema 已 commit、待跑 migration

---

## 系統現況

- **狀態**：Production 穩定（功能面 42/46 驗收通過）
- **URL**：https://onemood-expense-tracker.vercel.app
- **分支**：`claude/compassionate-cori-23fdb2`（worktree，最新 commit `6abac3b`）
- **資料庫**：Neon PostgreSQL，11 張表 → 即將擴為 13 張（+ invoices, invoice_lines）

---

## 本次 Session 進度

### 討論 → 決策

| 議題 | 結論 |
|------|------|
| 出帳對象 | 客戶（給設計師的獎金已在 `bonus_disbursed` 處理） |
| 記帳深度 | 月度 P&L 視圖即可，**不做雙分錄會計**（12 筆/年規模不必） |
| 單據粒度 | 一張請款單列**多案場**（`invoice_lines` 1:N） |
| 金額計算 | **半自動**：建議值 = `settlement_price - total_received`，可手動覆蓋 |
| `project_payments.invoice_id` | **加**（nullable，月對帳 SQL 一句搞定） |
| 編號格式 | `INV-YYYYMM-NNN` |
| Commit 拆分 | (1) Migration only → (2) Routes + Templates |

### Commit 1: `6abac3b` — schema 已寫入版控

| 檔案 | 內容 |
|------|------|
| `migrations/002_create_invoices.sql` | schema 文件版（純參考，跟 001 同風格） |
| `migrate_v5.py` | 對 Neon 執行的腳本（沿用 v4 pattern） |

**新增的表結構：**
- `invoices`：id, invoice_no, client_name, client_address, period_year/month, total_amount, status, issued_by/at, notes, audit fields
- `invoice_lines`：id, invoice_id (FK CASCADE), project_id (FK SET NULL), description, suggested_amount, amount, sort_order
- `project_payments` 加上 `invoice_id`（FK SET NULL，nullable）
- 5 個索引（period, status, invoice_id, project_id × 2）

---

## 下一步 — 阻塞點

### B1：跑 migration 到 Neon（**必須你親自跑**）

worktree 沒有 `.env.local`（被 .gitignore，正常），我拿不到 `POSTGRES_URL`。

**指令**（在你**有 `.env.local` 的工作目錄**執行）：
```bash
python migrate_v5.py
```

**預期輸出**：
```
[OK] invoices table created
[OK] invoice_lines table created
[OK] project_payments.invoice_id added
[OK] indexes created
Migration v5 done.
```

跑完回報「成功」，就接 Commit 2。

### B2：P0 阻塞點（從 TODO20260416Night 延續）

| # | 項目 | 動作 |
|---|------|------|
| P0-1 | Admin 密碼更換 | 登入 `/change-password` 改掉 `admin123` |
| P0-2 | Vercel Git 連結 | Dashboard → Settings → Login Connections → GitHub |
| P0-3 | SECRET_KEY preview | Git 連結後 `vercel env add SECRET_KEY preview` |

> 這三項與 invoices 模組獨立，但都需在 Production 部署前完成。

---

## 接著做（Commit 2 預定範圍）

待 migration 跑完後動工：

| 檔案 | 內容 |
|------|------|
| `routes/invoices.py` | Blueprint：list / new / detail / suggest API |
| `templates/invoices.html` | 請款單列表（含分頁） |
| `templates/invoice_form.html` | 新增/編輯（多選案場 → 帶建議金額） |
| `templates/invoice_detail.html` | 檢視 + status 流轉（draft → issued → paid） |
| `app.py` | 註冊 invoices blueprint |
| `services/utils.py`（若需） | `_generate_invoice_no()` helper（仿 `_generate_case_id`） |

**核心 helper**（沿用 `_get_project_summary` 的計算邏輯）：
```python
def suggest_invoice_amount(cur, project_id) -> Decimal:
    """建議請款金額 = settlement_price - total_received（已確認收款）"""
    summary = _get_project_summary(cur, project_id)
    return summary['remaining_balance']
```

---

## 接手應該先看的檔案

1. **這份檔案** — Session context
2. **`migrations/002_create_invoices.sql`** — schema 設計
3. **`migrate_v5.py`** — 待執行
4. **`routes/projects.py`** — 風格參考（Blueprint pattern + Decimal helpers）
5. **`services/utils.py`** — 共用工具（`get_conn`, `login_required`, `_d` 等）
6. **`HANDOFF20260416Night.md`** — 上一份 handoff（系統穩定狀態 baseline）

---

## 技術備註

- Decimal 精度：沿用既有 `_d()` helper，不另開新的
- 樂觀鎖：invoices 表也加 `updated_at`，未來編輯支援 `check_optimistic_lock`（但需擴展原 helper 支援多表）
- 審計：status 變更（issued / paid / void）寫入 `audit_logs`
- 權限：admin 全控；設計師預設只能看自己案場相關的請款單（待確認需求）

---

## 待你決策（Commit 2 開工前可答可不答）

1. **設計師能不能看請款單？** — 預設我會做成 admin only，避免複雜化
2. **PDF 輸出要做嗎？** — 我建議 Commit 2 不做，留到驗證 schema OK 之後再加（用 WeasyPrint 或瀏覽器列印）
3. **付款狀態變更要不要寫 audit_log？** — 我建議要，沿用現有 `write_audit_log`
