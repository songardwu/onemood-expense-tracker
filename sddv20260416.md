# Onemood 案場損益管理系統 — System Design Document
> Version: 2026-04-16 | Status: Production (Vercel)

---

## 1. 系統概述

Onemood 是為室內設計公司打造的案場損益管理暨費用報帳系統。核心目標：

- **報帳管理**：設計師線上提報費用，管理員審核、匯出匯款單
- **案場損益**：追蹤每案場合約收入、成本支出、分潤結算
- **廠商管理**：維護匯款帳戶資料，自動帶入報帳表單
- **使用者管理**：角色權限（admin / designer）、登入追蹤

---

## 2. 技術架構

| 層級 | 技術 |
|------|------|
| 後端 | Python 3.x + Flask (Blueprint) |
| 模板 | Jinja2 SSR（無 SPA / 無前端框架） |
| 資料庫 | PostgreSQL (Neon hosted + PgBouncer pooler) |
| 部署 | Vercel Serverless (`api/index.py` 入口) |
| 安全 | flask-wtf (CSRF) + flask-limiter (Rate Limit) + werkzeug (password hash) |
| 匯出 | pandas + openpyxl（Excel 產生 / 匯入） |

### 2.1 部署架構

```
Browser → Vercel Edge → Serverless Function (api/index.py)
                              ↓
                        Flask App (app.py)
                              ↓
                    Neon PostgreSQL (PgBouncer pooler)
```

### 2.2 專案結構

```
├── api/index.py              # Vercel serverless 入口
├── app.py                    # Flask 主程式（設定、中介層、Blueprint 註冊）
├── routes/
│   ├── __init__.py
│   ├── auth.py               # 登入/登出/改密碼/登入紀錄
│   ├── reports.py            # 報帳 CRUD / 搜尋篩選 / 匯出 / 加總
│   ├── vendors.py            # 廠商 CRUD / 匯入匯出 / 軟刪除
│   ├── users.py              # 使用者 CRUD
│   └── projects.py           # 案場損益（收入/押金/成本/分潤）
├── services/
│   ├── __init__.py
│   └── utils.py              # DB 連線 / 認證裝飾器 / 分頁 / 樂觀鎖 / 審計
├── templates/                # 14 個 Jinja2 模板
│   ├── base.html             # 共用佈局（navbar / CSS / Dark Mode）
│   ├── login.html
│   ├── list.html             # 報帳列表
│   ├── new.html              # 新增報帳
│   ├── vendors.html          # 廠商管理
│   ├── users.html            # 使用者管理
│   ├── projects.html         # 案場列表
│   ├── project_detail.html   # 案場明細（收入/押金/成本/損益）
│   ├── project_form.html     # 新增案場
│   ├── project_logs.html     # 案場修改紀錄
│   ├── cost_categories.html  # 成本科目管理
│   ├── change_password.html
│   ├── login_logs.html
│   └── pagination.html       # 分頁元件（include）
├── static/
│   └── style.css             # 暖大地色系 Design Token + Bento Grid + Dark Mode
├── migrations/
│   └── 001_create_projects.sql
├── migrate_v2.py             # users 表 + reports.user_id
├── migrate_v3.py             # reports 鎖定欄位 + vendor_keywords
└── migrate_v4.py             # vendors 表 + reports.payment_method
```

---

## 3. 資料庫 Schema

### 3.1 reports（報帳紀錄）

| 欄位 | 型別 | 說明 |
|------|------|------|
| id | SERIAL PK | |
| vendor | TEXT NOT NULL | 廠商名稱 |
| vendor_type | TEXT NOT NULL | 廠商類型 |
| amount | NUMERIC NOT NULL | 金額 |
| category | TEXT NOT NULL | 案場成本 / 管銷 / 獎金 |
| invoice_no | TEXT | 發票號碼（可空） |
| invoice_date | DATE NOT NULL | 發票日期 |
| remit_date | DATE | 預設匯款日 |
| project_no | TEXT NOT NULL | 案件編號 |
| stage | TEXT | 階段 |
| user_id | INTEGER NOT NULL FK→users | 提報人 |
| payment_method | VARCHAR(20) | 付款方式 |
| is_locked | BOOLEAN DEFAULT FALSE | 鎖定狀態 |
| updated_by | INTEGER FK→users | 修改人 |
| updated_at | TIMESTAMP | 修改時間 |
| created_at | TIMESTAMP DEFAULT NOW() | |

### 3.2 users（使用者）

| 欄位 | 型別 | 說明 |
|------|------|------|
| id | SERIAL PK | |
| username | VARCHAR(50) UNIQUE NOT NULL | 帳號 |
| display_name | VARCHAR(100) NOT NULL | 顯示名稱 |
| password_hash | VARCHAR(255) NOT NULL | bcrypt hash |
| role | VARCHAR(20) NOT NULL | 'designer' / 'admin' |
| is_active | BOOLEAN DEFAULT TRUE | 啟停用 |
| created_at | TIMESTAMP DEFAULT NOW() | |

### 3.3 vendors（廠商）

| 欄位 | 型別 | 說明 |
|------|------|------|
| id | SERIAL PK | |
| name | VARCHAR(200) UNIQUE NOT NULL | 廠商名稱 |
| bank_name | VARCHAR(200) NOT NULL | 銀行分行 |
| bank_code | VARCHAR(50) NOT NULL | 銀行代碼 |
| account_no | VARCHAR(50) NOT NULL | 帳號 |
| account_name | VARCHAR(200) NOT NULL | 戶名 |
| is_active | BOOLEAN DEFAULT TRUE | 軟刪除旗標 |
| created_by | INTEGER FK→users | |
| created_at | TIMESTAMP DEFAULT NOW() | |
| updated_by | INTEGER FK→users | |
| updated_at | TIMESTAMP | |

### 3.4 vendor_keywords（廠商關鍵字）

| 欄位 | 型別 | 說明 |
|------|------|------|
| id | SERIAL PK | |
| keyword | VARCHAR(50) UNIQUE NOT NULL | 如「公司」「工作室」等 |

用於廠商名稱相似偵測時，去除通用關鍵字後比對核心名稱。

### 3.5 projects（案場主檔）

| 欄位 | 型別 | 說明 |
|------|------|------|
| id | SERIAL PK | |
| case_id | VARCHAR(20) UNIQUE NOT NULL | CASE-YYYYMMDD-NNN |
| case_name | VARCHAR(200) NOT NULL | 案名 |
| owner_name | VARCHAR(100) | 業主 |
| owner_phone | VARCHAR(30) | 電話 |
| owner_address | VARCHAR(300) | 地址 |
| contract_date | DATE | 簽約日 |
| construction_start | DATE | 開工日 |
| construction_end | DATE | 完工日 |
| status | VARCHAR(20) DEFAULT 'active' | active / completed / closed |
| designer_id | INTEGER NOT NULL FK→users | 負責設計師 |
| system_furniture_amount | NUMERIC(12,2) DEFAULT 0 | 系統工程合約金額 |
| non_system_furniture_amount | NUMERIC(12,2) DEFAULT 0 | 非系統工程合約金額 |
| tax_amount | NUMERIC(12,2) DEFAULT 0 | 營業稅 |
| deposit_amount | NUMERIC(12,2) DEFAULT 0 | 裝修押金 |
| deposit_refund | NUMERIC(12,2) DEFAULT 0 | 退還金額 |
| deposit_status | VARCHAR(20) DEFAULT 'pending' | pending / partial / refunded |
| profit_share_pct | NUMERIC(5,2) DEFAULT 0 | 設計師分潤比 |
| bonus_checked | BOOLEAN DEFAULT FALSE | 管理者核對 |
| bonus_disbursed | BOOLEAN DEFAULT FALSE | 已出帳 |
| bonus_report_id | INTEGER FK→reports | 關聯報帳紀錄 |
| created_at | TIMESTAMP DEFAULT NOW() | |
| updated_at | TIMESTAMP DEFAULT NOW() | |

### 3.6 project_adjustments（追加減明細）

| 欄位 | 型別 | 說明 |
|------|------|------|
| id | SERIAL PK | |
| project_id | INTEGER FK→projects ON DELETE CASCADE | |
| adjust_date | DATE | |
| description | VARCHAR(300) | |
| amount | NUMERIC(12,2) NOT NULL | 正=追加、負=減少 |
| created_at | TIMESTAMP DEFAULT NOW() | |

### 3.7 project_discounts（折讓/扣抵）

| 欄位 | 型別 | 說明 |
|------|------|------|
| id | SERIAL PK | |
| project_id | INTEGER FK→projects ON DELETE CASCADE | |
| item_name | VARCHAR(200) NOT NULL | |
| amount | NUMERIC(12,2) NOT NULL | 折讓金額 |
| created_at | TIMESTAMP DEFAULT NOW() | |

### 3.8 project_payments（收款明細）

| 欄位 | 型別 | 說明 |
|------|------|------|
| id | SERIAL PK | |
| project_id | INTEGER FK→projects ON DELETE CASCADE | |
| payment_date | DATE NOT NULL | |
| payment_method | VARCHAR(20) NOT NULL | 現金 / 匯款 / 其他 |
| amount | NUMERIC(12,2) NOT NULL | |
| is_confirmed | BOOLEAN DEFAULT FALSE | 對帳確認 |
| confirmed_by | INTEGER FK→users | |
| confirmed_at | TIMESTAMP | |
| created_at | TIMESTAMP DEFAULT NOW() | |

### 3.9 cost_categories（成本科目）

| 欄位 | 型別 | 說明 |
|------|------|------|
| id | SERIAL PK | |
| name | VARCHAR(100) NOT NULL | |
| cost_type | VARCHAR(20) NOT NULL | system / non_system |
| sort_order | INTEGER DEFAULT 0 | 排序 |
| is_active | BOOLEAN DEFAULT TRUE | |

預設 6 系統 + 9 非系統科目。

### 3.10 project_costs（案場成本）

| 欄位 | 型別 | 說明 |
|------|------|------|
| id | SERIAL PK | |
| project_id | INTEGER FK→projects ON DELETE CASCADE | |
| category_id | INTEGER FK→cost_categories | |
| amount | NUMERIC(12,2) DEFAULT 0 | |
| UNIQUE(project_id, category_id) | | |

### 3.11 audit_logs（審計日誌）

| 欄位 | 型別 | 說明 |
|------|------|------|
| id | SERIAL PK | |
| table_name | VARCHAR(50) NOT NULL | |
| record_id | INTEGER NOT NULL | |
| field_name | VARCHAR(100) NOT NULL | |
| old_value | TEXT | |
| new_value | TEXT | |
| changed_by | INTEGER FK→users | |
| changed_at | TIMESTAMP DEFAULT NOW() | |
| reason | TEXT | 如解鎖原因 |

### 3.12 login_logs（登入紀錄）

| 欄位 | 型別 | 說明 |
|------|------|------|
| id | SERIAL PK | |
| user_id | INTEGER FK→users | |
| username | VARCHAR(50) | |
| success | BOOLEAN | |
| ip_address | VARCHAR(45) | |
| created_at | TIMESTAMP DEFAULT NOW() | |

---

## 4. 安全機制

| 機制 | 實作 |
|------|------|
| CSRF | flask-wtf CSRFProtect，所有 POST 表單含 `csrf_token()` |
| Rate Limit | flask-limiter，登入 5/min，全域 120/min |
| Session | HttpOnly + SameSite=Lax + Secure(Vercel) |
| 密碼 | werkzeug bcrypt hash，最低 6 字元 |
| 權限 | `@login_required` / `@admin_required` 裝飾器 |
| Session Fixation | 登入前 `session.clear()` |
| HTTP Headers | X-Content-Type-Options: nosniff, X-Frame-Options: DENY, CSP |
| 廠商匯入 | 檔案 ≤ 2MB、≤ 500 筆、格式 .xlsx/.csv |
| 樂觀鎖 | `expected_updated_at` 比對防併發覆寫（案場編輯） |
| 軟刪除 | vendors.is_active 旗標取代硬刪除 |

---

## 5. 核心功能模組

### 5.1 報帳模組 (`routes/reports.py`)
- 新增 / 編輯 / 鎖定報帳紀錄
- 搜尋篩選（日期區間 / 廠商 / 案場 / 類別）
- SQL 分頁（LIMIT/OFFSET + COUNT），50 筆/頁
- 廠商加總（GROUP BY vendor + payment_method）
- Excel 匯出（含篩選條件匯出）
- 台灣假日工作日計算（預設匯款日：次月 5 日遇假順延）

### 5.2 案場損益模組 (`routes/projects.py`)
- 案場基本資料 CRUD（自動流水號 CASE-YYYYMMDD-NNN）
- 合約收入：系統/非系統工程金額 + 營業稅
- 追加減管理：新增/刪除明細項目
- 折讓/扣抵管理
- 收款管理：付款日期/方式/金額 + 對帳確認
- 裝修押金：收取/退還/狀態追蹤
- 成本管理：15 個預設科目，金額 upsert
- 損益計算：毛利、利潤率、設計師獎金（Decimal 精度）
- 獎金出帳：JS confirm → 建立報帳紀錄 → 標記已出帳
- 樂觀鎖防併發覆寫

### 5.3 廠商模組 (`routes/vendors.py`)
- 新增 / 行內編輯 / 軟刪除（啟用/停用）
- Excel/CSV 批次匯入（Upsert：新增 or 更新）
- 範本下載
- API：`/api/vendor-bank?name=` 帶入匯款資訊
- 廠商名稱相似偵測（去除關鍵字後核心字串比對）
- Flash message 顯示匯入結果

### 5.4 使用者模組 (`routes/users.py`)
- 新增使用者（admin 限定）
- 停用/啟用帳號
- 修改密碼（自己改自己）

### 5.5 認證模組 (`routes/auth.py`)
- 登入/登出
- 登入紀錄查看（IP 追蹤）
- 修改密碼

---

## 6. UI/UX 設計

| 項目 | 規格 |
|------|------|
| 設計系統 | 暖大地色系 Design Token |
| 排版 | Bento Grid 全頁面 |
| 字型 | Noto Serif TC (標題) + Noto Sans TC (內文) |
| Dark Mode | 暖棕色版，prefers-color-scheme 自動切換 |
| 響應式 | Desktop table ↔ Mobile cards 切換 |
| 導航 | 手機 hamburger nav |
| 無障礙 | Skip link / semantic HTML / aria-label / scope col |
| 動態 | prefers-reduced-motion guard |
| 列印 | Print stylesheet |
| iOS | input font-size ≥ 16px 防縮放 |

---

## 7. 環境變數

| 變數 | 用途 |
|------|------|
| `SECRET_KEY` | Flask session 簽名金鑰 |
| `POSTGRES_URL` / `DATABASE_URL` | Neon PostgreSQL 連線字串 |
| `VERCEL` | 偵測 Vercel 環境（啟用 Secure cookie） |
| `FLASK_DEBUG` | 開發模式 debug=True |

---

## 8. 已知限制

- DB 連線使用 Neon PgBouncer pooler，不支援 LISTEN/NOTIFY
- Vercel Serverless 無持久記憶體，flask-limiter 使用 `memory://`（跨 instance 不共享）
- 廠商相似偵測為伺服器端比對，非即時前端提示
- 報帳紀錄的 `vendor` 為純文字，非 FK 關聯 vendors 表
- 台灣假日資料僅到 2027 年底，需定期更新
- 測試腳本因 CSRF 保護無法直接以 HTTP client 執行
